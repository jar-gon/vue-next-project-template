/**
 * Configure and register global directives
 */
import type { App } from 'vue'
// import { ElLoading } from 'element-plus'

export function setupGlobDirectives(app: App) {
  // app.use(ElLoading)
  console.log('setupGlobDirectives', app)
}
